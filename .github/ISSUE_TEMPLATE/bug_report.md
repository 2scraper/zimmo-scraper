---
name: Bug report
about: Something the scraper does (or fails to do) that shouldn't happen
labels: bug
---

**Command run** (redact any real API key / proxy credential before pasting):

```
python3 playwright_scraper.py --url "..." ...
```

**What happened** (full output, or the relevant excerpt):

```
paste here
```

**What you expected instead**:

**If this is about wrong/missing DATA in the output** (not a crash):
- Listing URL:
- Field that was wrong:
- What the field should have been (screenshot or exact text from the page):

**Environment**:
- Engine: Playwright / Puppeteer / Selenium / Scraper API
- OS:
- Python version: `python3 --version`
- Output of `python3 env_config.py` (this prints sources, never secret values, so it's safe to paste)
